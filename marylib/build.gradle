apply plugin: 'com.android.library'
apply plugin: 'com.jfrog.bintray'
apply plugin: 'com.github.dcendents.android-maven'

android {
    compileSdkVersion 23
    buildToolsVersion "24.0.0"


    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 23
        multiDexEnabled true

        android {
            packagingOptions {
                exclude 'META-INF/LICENSE.txt'
                exclude 'META-INF/NOTICE.txt'
                exclude 'META-INF/LICENSE'
                exclude 'META-INF/NOTICE'
                exclude 'META-INF/DEPENDENCIES'
                exclude 'jtok/de/de_b-abbrev.txt'
                exclude 'jtok/en/en_b-abbrev.txt'
            }
            lintOptions {
                disable 'InvalidPackage'
                abortOnError false
            }

        }
        compileOptions {
            sourceCompatibility JavaVersion.VERSION_1_7
            targetCompatibility JavaVersion.VERSION_1_7
        }

    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }


    dexOptions {
        javaMaxHeapSize "2g"
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }
}

dependencies {
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:support-v4:23.4.0'
    compile 'org.apache.opennlp:opennlp-maxent:3.0.3'
    compile 'org.apache.opennlp:opennlp-uima:1.5.3'
    compile 'org.apache.opennlp:opennlp-tools:1.5.3'
    compile 'com.android.support:multidex:1.0.0'
    compile 'net.sf.jwordnet:jwnl:1.3.3'
    compile 'com.ibm.icu:icu4j:57.1'
    compile fileTree(include: ['*.jar'], dir: 'libs')
}


group = 'com.marytts.android'
version = '0.1.2'

task generateSourcesJar(type: Jar) {
    from android.sourceSets.main.java.srcDirs
    classifier 'sources'
}

task generateJavaDocs(type: Javadoc) {
    source = android.sourceSets.main.java.srcDirs
    classpath += project.files(android.getBootClasspath()
            .join(File.pathSeparator))
}

task generateJavaDocsJar(type: Jar) {
    from generateJavaDocs.destinationDir
    classifier 'javadoc'
}
generateJavaDocsJar.dependsOn generateJavaDocs

bintray {
    user = 'farizaghayev'
    key = 'd54c77f7507446a76a3a787e60a26f15a1ccad14'
    pkg {
        repo = 'maven'
        name = 'com.marytts.android.lib'

        version {
            name = '0.1.2'
            desc = 'Mary lib upload'
            released = new Date()
            vcsTag = '0.1.2'
        }

        licenses = ['Apache-2.0']
        vcsUrl = 'https://github.com/AndroidMaryTTS/AndroidMaryTTS.git'
        websiteUrl = 'https://github.com/AndroidMaryTTS/AndroidMaryTTS'
    }
    configurations = ['archives']
}

artifacts {
//    archives generateJavaDocsJar
    //   archives generateSourcesJar
}